import './App.css';
import { BrowserRouter, Route, Routes } from "react-router-dom";
import styled

const App = () => {
    return(
        <BrowserRouter>
            <Routes>
                <Route path
            </Routes>
        </BrowserRouter>
        );
}


export default App;
